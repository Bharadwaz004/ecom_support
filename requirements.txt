# Container runtime. Must stay light enough for 512MB: no torch, no sentence-transformers,
# no transformers, no pandas, and deliberately no qdrant-client (grpcio + protobuf for two
# REST calls). Laptop-only dependencies live in requirements-dev.txt.
fastapi==0.140.6
uvicorn[standard]==0.51.0
mcp==1.28.1
openai==2.48.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Pinned because a resolver drift here is what actually breaks builds: starlette is shared
# by fastapi and mcp, and sse-starlette/httpx-sse carry the streamable HTTP transport.
starlette==1.3.1
sse-starlette==3.4.6
httpx-sse==0.4.3
anyio==4.14.2
